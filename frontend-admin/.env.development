# Local development: the API and the assets are served on different
# ports than the panel. Vite only loads this file in dev mode; the
# production build falls back to relative paths.
VITE_API_BASE_URL=http://localhost:8080/api/v1
VITE_ASSETS_BASE_URL=http://localhost:3000/assets/
