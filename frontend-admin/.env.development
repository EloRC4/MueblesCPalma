# Entorno de desarrollo local: la API y los assets se sirven en
# puertos distintos al del panel. Vite solo carga este fichero en
# modo desarrollo; el build de producción usa rutas relativas.
VITE_API_BASE_URL=http://localhost:8080/api/v1
VITE_ASSETS_BASE_URL=http://localhost:3000/assets/
